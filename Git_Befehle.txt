git init - initialisieren eines neuen lokalen repositories
git add Dateiname - fügt die definierte datei dazu ( staging area)
git add * - fügt alle änderungen hinzu (staging area)
git config --global user.name "JanineSallmann"
git config --global user.email "email"